import 'package:flutter/material.dart';
class AppColors{
  static const primary=Color(0xFF78A6C8); //for main buttons
  static const white =Color(0xFFE9EEF2); // background and the color of word in primary buttons
  static const black =Color(0xFF05141F);//for background in dark and words in buttons=dark mode + names
  static const grey=Color(0xFF9CADBC); // for border + names like Admin
  static const orange=Color(0xFFE65C4F);//not for now
}